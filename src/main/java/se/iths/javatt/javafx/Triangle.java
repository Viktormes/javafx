package se.iths.javatt.javafx;

import javafx.scene.paint.Color;

public class Triangle extends Shape{
    public Triangle(int size, Color color, double x, double y) {
        super(size, color, x, y);
    }
}
